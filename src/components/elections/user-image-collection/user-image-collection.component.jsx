import './user-image-collection.css'
import UserImage from '../user-image/user-image.component'

const UserImageCollection = ({imgs}) => {
    return <div className='user-image-collection'>
        {/* {imgs.map(img => <UserImage img={img}></UserImage>)} */}
    </div>
}

export default UserImageCollection